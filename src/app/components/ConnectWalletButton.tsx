'use client'

import React from "react";
import MyButton from "./MyButton";

interface Props {

}

export const ConnectWalletButton: React.FC<Props> = ({ }) => {
    return <MyButton>Connect Wallet</MyButton>;
}